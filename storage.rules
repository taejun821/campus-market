rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {

    // 이미지는 누구나 읽기 가능 (공개 URL 방식)
    match /images/{allPaths=**} {
      allow read: if true;
      // 쓰기는 서버(Admin SDK)만 허용 - 클라이언트 직접 업로드 차단
      allow write: if false;
    }
  }
}
