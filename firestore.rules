rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {

    // 모든 읽기/쓰기는 Spring Boot 서버(Admin SDK)가 처리하므로 클라이언트 직접 접근 차단
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
